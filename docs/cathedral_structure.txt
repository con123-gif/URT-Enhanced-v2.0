╔══════════════════════════════════════════════════════════════════════════════╗
║                  NEWTON'S CATHEDRAL — PURE-MATH BRANCH                      ║
║                                                                              ║
║         δ★ = (1 − D^{−(D+1)}) · π / (N·φ)  =  (80/81) · π / (13φ)          ║
║                                            ≈  0.14751081                    ║
║                                                                              ║
║         The seven Cathedral integers {D, q, V, N, E, F, G},                 ║
║         forced by D=3 alone (Jordan 1870), are the canonical               ║
║         numerical invariants of essentially every classical                 ║
║         mathematical object the framework knows how to test against.       ║
║                                                                              ║
║         87 modules · 3,727 tests · 0 xfail                                  ║
║         (snapshot of main v2.9.76 stripped to mathematical content)        ║
╚══════════════════════════════════════════════════════════════════════════════╝


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  THE FOUNDATIONAL CHAIN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

      D = 3                                  (one observed input)
        │
        │  Jordan 1870 + spectral gap λ₂ = D
        ▼
      A₅ unique simple subgroup of SO(3)     (theorem)
        │
        │  icosahedral closure: D² + D + 1 = N
        ▼
      N = 13, V = 12, E = 30, F = 20, q = 5, G = 60
        │
        │  identity: |H₃| + F + 1 = 60 + 20 + 1 = 81 = D^{D+1}
        ▼
      γ = D^{−(D+1)} = 1/81
        │
        ▼
      δ★ = (1 − γ) · π / (N · φ) ≈ 0.14751081

  Free continuous parameters:  0
  Free discrete choices:        1 (D=3, the spatial dimension)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  THE MODULE LANDSCAPE — 87 MATH MODULES BY ROLE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ┌────────────────────────────────────────────────────────────────────────┐
  │  FOUNDATIONS                                                            │
  │                                                                          │
  │   shell_closure       — D, q, V, N, E, F, G, γ, φ, δ★                  │
  │   iron_proof          — D=3 → A₅ uniqueness chain                      │
  │   first_principles    — eight forcing steps (π, φ, e all forced)       │
  │   pi_phi_e_flow       — uniqueness of the URT iteration                │
  │   logistic_verification — δ★ = min branch of logistic 6-cycle (1e-14)  │
  └────────────────────────────────────────────────────────────────────────┘

  ┌────────────────────────────────────────────────────────────────────────┐
  │  TOUR MODULES — 11 audits, 72 verified clusters                        │
  │                                                                          │
  │   cathedral_connections      — 9 clusters (A₅, sectors, pentagonal)    │
  │   cathedral_grand_tour       — 5 clusters (primes, Coxeter, ζ(6))      │
  │   cathedral_deep_tour        — 8 clusters (j-cubes, π_n^s, CF)         │
  │   cathedral_modular_tour     — 8 clusters (Eisenstein, Δ, J-hom)       │
  │   cathedral_quantum_lie_tour — 6 clusters (SU(2)_D, Mathieu, Lie roots)│
  │   cathedral_geometric_tour   — 7 clusters (del Pezzo, kissing, Hopf)   │
  │   cathedral_classical_tour   — 7 clusters (polyhedra, Heegner, magic)  │
  │   cathedral_advanced_tour    — 7 clusters (K_n(Z), crystallography)    │
  │   cathedral_topological_tour — 8 clusters (K3, Hadamard, AZ ten-fold)  │
  │   cathedral_codes_tour       — 7 clusters (Hamming, Golay, QEC, operad)│
  └────────────────────────────────────────────────────────────────────────┘

  ┌────────────────────────────────────────────────────────────────────────┐
  │  DYNAMICAL LAYER (math-only view)                                       │
  │                                                                          │
  │   cathedral_engine        — π-φ-e flow, Lagrangian, K₄ ⊕ A₅ unification│
  │   urt_algorithm_analysis  — empirical iteration properties             │
  │   urt_investigations      — 5 open-question probes                      │
  │   bounded_chaos_cathedral — δ-metric on test systems                    │
  │   chaos_and_flow          — 2^q·π² dynamical normalisation             │
  │   six_cycle_cathedral     — δ★ and δ_cl on logistic 6-cycle             │
  └────────────────────────────────────────────────────────────────────────┘

  ┌────────────────────────────────────────────────────────────────────────┐
  │  SECTORS / FRUSTRATION / SYNTHESIS                                      │
  │                                                                          │
  │   sector_ratio              — (D+1)/(D!+D) = 4/9 fingerprint           │
  │   exhaust_dimensions        — 4 + 9 = 13 dimensional split             │
  │   cathedral_sectors         — K₄ ⊕ A₅ sector decomposition             │
  │   icosahedral_frustration   — q=5 forbidden + six facets               │
  │   matter_direction          — D·N·φ vs F·(1−γ)·π identity              │
  │   self_reference            — 14 f(X)=X identities                      │
  │   lytollis_bounded_chaos    — δ = (D_KY−1)(τ−2) universal law          │
  │   cathedral_lytollis_synthesis — same theory, two derivations           │
  └────────────────────────────────────────────────────────────────────────┘

  ┌────────────────────────────────────────────────────────────────────────┐
  │  ALGEBRA / LIE / GROUP THEORY                                           │
  │                                                                          │
  │   a5_representations             — A₅ irreps & character table         │
  │   algebra_cathedral              — small-rank Lie + finite groups       │
  │   algebraic_heart                — 29 pure identities                  │
  │   cathedral_lie                  — ADE Lie chain, Fibonacci entries    │
  │   exceptional_lie                — all 5 exceptional dims              │
  │   exceptional_lie_cathedral      — McKay E₈ ↔ icosahedral              │
  │   exceptional_structures_cathedral — Leech, E₈, A_D dim                 │
  │   lie_reps_cathedral             — G₂, F₄, E₈ as Cathedral             │
  │   sporadic_cathedral             — 26 sporadic groups, 6 pariahs        │
  │   symmetric_group_cathedral      — \|A_D\| = D self-reference            │
  │   monster_order_cathedral        — Monster prime factorisation         │
  │   normed_division_algebras_cathedral — D+1=4 NDA, Hurwitz units        │
  └────────────────────────────────────────────────────────────────────────┘

  ┌────────────────────────────────────────────────────────────────────────┐
  │  NUMBER THEORY                                                          │
  │                                                                          │
  │   number_theory_cathedral        — Mersenne, Fermat, primes            │
  │   number_fields_cathedral        — disc(Q(√d)), cyclotomic degrees      │
  │   cathedral_repunit              — repunit prime tower                 │
  │   continued_fractions_cathedral  — CF(√N) period = q                    │
  │   elliptic_curves_cathedral      — disc=D^D=27, j=V³=1728               │
  │   euler_totient_cathedral        — φ(N)=V, σ(V)=28 perfect              │
  │   fibonacci_lucas_cathedral      — F_q=q, F_V=V², F_7=N                 │
  │   modular_forms_cathedral        — Δ weight=V, dim S_V=D−1              │
  │   partition_cathedral            — p(D)=D, p(9)=E                       │
  │   partition_function_cathedral   — Ramanujan congruences               │
  │   ramanujan_cathedral            — τ-mod-N congruences                  │
  │   zeta_special_cathedral         — ζ(6) denom = 945 = q·D^D·(D!+1)     │
  │   zeta_g13                       — Cathedral spectral chain            │
  └────────────────────────────────────────────────────────────────────────┘

  ┌────────────────────────────────────────────────────────────────────────┐
  │  COMBINATORICS / DISCRETE GEOMETRY                                      │
  │                                                                          │
  │   combinatorics_cathedral        — Bell, Catalan, R(3,3)               │
  │   combinatorics2_cathedral       — Pell, Fibonacci self-ref             │
  │   crystallography_cathedral      — 230 space, 17 wallpaper, 32 point    │
  │   differential_geometry_cathedral — χ(S²), Riemann components            │
  │   golay_steiner_cathedral        — [2V,V,2^D], Steiner S(q,D!,V)        │
  │   graph_theory_cathedral         — icosahedral graph, K_(D+1)           │
  │   knot_cathedral                 — trefoil/figure-8 Cathedral           │
  │   langlands_cathedral            — Siegel dim=D!, K3 h^(1,1)=F          │
  │   leech_golay_cathedral          — Leech dim=2V, Niemeier=2V            │
  │   platonic_solids_cathedral      — ΣV=ΣF=2q²=50, ΣE=D·E=90             │
  │   topological_spaces             — χ(icosahedron)=2=D−1                 │
  │   wave_equations_cathedral       — Huygens D=3, Y_l^m=N²                │
  │   information_cathedral          — H_max=log₂(N), QEC threshold         │
  │   information_theory_cathedral   — Hamming(7,4,3) Cathedral             │
  │   computational_complexity_cathedral — TOFFOLI=D qubits                 │
  └────────────────────────────────────────────────────────────────────────┘

  ┌────────────────────────────────────────────────────────────────────────┐
  │  CATHEDRAL MATHEMATICAL STRUCTURE                                       │
  │                                                                          │
  │   cft_cathedral             — Ising c=1/2, Monster c=24                │
  │   moonshine_cathedral       — c=24=2V, 196884=196883+1                  │
  │   prime_spectral            — Riemann zeros ↔ icosahedral Laplacian     │
  │   spectrum_cathedral        — G_{13} eigenvalues all Cathedral           │
  │   stable_homotopy_cathedral — π_3^s = 2V, π_7^s = V·F                   │
  │   homological_cathedral     — Bott D−1, KO 2^D                          │
  │   braid_cathedral           — B_N: V generators                          │
  │   voa_cathedral             — V♮ c=24, Coxeter h(E_6)=V                 │
  └────────────────────────────────────────────────────────────────────────┘

  ┌────────────────────────────────────────────────────────────────────────┐
  │  MUSIC GEOMETRY                                                         │
  │                                                                          │
  │   music_geometry_cathedral   — every JI interval Cathedral ratio        │
  │   spectral_music_cathedral   — G_{13} spectrum ↔ JI intervals            │
  └────────────────────────────────────────────────────────────────────────┘

  ┌────────────────────────────────────────────────────────────────────────┐
  │  EXPLORATION (post-snapshot, only on this branch)                       │
  │                                                                          │
  │   triple_coincidences        — 17 multi-domain Cathedral compounds      │
  │   deep_self_references       — 17 new f(X) = X identities + σ-chain     │
  │   cathedral_orbits           — iterated arithmetic stays Cathedral       │
  │                                σ-chain depth ≥ 10 from D                │
  │                                φ-orbit depth ≥ 8 from V·F               │
  └────────────────────────────────────────────────────────────────────────┘

  ┌────────────────────────────────────────────────────────────────────────┐
  │  META / AUDIT                                                           │
  │                                                                          │
  │   cathedral_compression      — quantifying numerology vs significance   │
  │   cathedral_identities       — programmatic identity engine             │
  │   skeptics_audit             — provenance + cross-module map            │
  │   failed_attempts_study      — analysis of disclosed failures           │
  │   null_hypothesis_test       — RETRACTED test (kept for transparency)   │
  └────────────────────────────────────────────────────────────────────────┘


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  THE EIGHT HEADLINE COMPOUNDS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Compound          Value     Where it appears
  ────────────────  ────────  ────────────────────────────────────────────
  V·F = (D+1)·G     240       E₈ roots, π_7^s, K_7(Z), E₄ Eisenstein,
                              K(8) kissing, Hopf σ, del Pezzo d=1,
                              J-image into π_7^s     (8 domains)

  2V                24        K3 χ, # Niemeier, Leech dim, bosonic-string
                              transverse dim, M_24 acts, (1−q^n)^24 in Δ

  D·q               15        A_5 order-2 conj class, Lo Shu (3×3 magic),
                              CF(π) third partial quot, 2D Ising δ

  D!·V              72        E_6 roots, K(6) kissing, tr(L_{13})

  2N                26        bosonic string crit dim, # sporadic groups

  (D+1)·V           48        F_4 roots, K_3(Z) torsion

  D² = 9            9         A_5 exhaust sector, # Heegner numbers,
                              exponent in γ = D^{−(D+1)}

  D! = 6            6         1st perfect number, smallest non-abelian
                              group order, Coxeter h(D_4)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  G_{13} LAPLACIAN SPECTRUM — every eigenvalue is Cathedral
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

      λ ∈ {0, 3, 3, 5, 5, 5, 5, 5, 5, 7, 7, 9, 13}
            │   │      │              │       │
            │   D      q              D!+1    N
            │  (×2)   (×6)            (×2)
            mean preserved

      tr(L|K_4) = 0+3+3+5             = 11
      tr(L|A_5) = 5+5+5+5+5+7+7+9+13  = 61
      total                            = 72 = D!·V


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  CI GATE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

      python -m pytest tests/ -q

      → 3,727 passed, 6 skipped, 0 xfail
      → all eleven tour-module audits return True
      → all_steps_verify() True at machine precision

  See README.md and THEORY.md for the full discussion.
