"""
G₁₃ Cathedral (newtons_cathedral)

Pure mathematical framework for structured emergence.

Core objects: G₁₃ centered icosahedral graph, URT contraction flow,
Lytollis’s Law robustness margin, spectral geometry, and derived
mathematical structures (including a derived Lorentzian signature
from K₄ modes).

Focus: patterns (symmetry sectors, degenerate modes), structure
(external/internal hierarchy), function (contraction/relaxation to
bounded non-trivial attractors).

This branch (grok-review) emphasizes rigorous geometry and dynamics
over physical interpretations. Physics quantities, if they appear,
are treated as possible downstream mappings, not primary results.

Public API exports both core mathematical primitives and a large
set of derived quantities (some of which have been historically
compared to physical observables).

CI top-level gate:
    from newtons_cathedral import all_audits_pass
    assert all_audits_pass()
"""